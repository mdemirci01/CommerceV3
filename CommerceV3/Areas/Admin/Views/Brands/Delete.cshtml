@model CommerceV3.Models.Brand

@{
    ViewData["Title"] = "Marka Sil: " + Model.Name;
}

<h3>Silmek istediğinize emin misiniz?</h3>
<div>
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Name)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.Name)
        </dd>
        <dt>
            @Html.DisplayNameFor(model => model.Logo)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.Logo)
        </dd>
    </dl>
    
    <form asp-action="Delete">
        <input type="hidden" asp-for="Id" />
        <input type="submit" value="Sil" class="btn btn-sm btn-danger" /> 
        <a asp-action="Index" class="btn btn-sm btn-default">İptal</a>
    </form>
</div>
